# Dashboards

The primary dashboard is implemented in `app/app.py` with Streamlit.

Run it after training:

```bash
streamlit run app/app.py
```

The dashboard uses the saved model bundle in `models/fraud_detection_model.joblib` and generated analytics from `reports/`.

